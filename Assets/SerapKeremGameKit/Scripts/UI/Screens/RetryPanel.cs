namespace SerapKeremGameKit._UI
{
    /// <summary>Legacy type alias — implementation lives in <see cref="_Game.UI.RetryPanel"/>.</summary>
    public sealed class RetryPanel : _Game.UI.RetryPanel
    {
    }
}
